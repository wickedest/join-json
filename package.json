{
  "name": "join-json",
  "version": "1.0.7",
  "description": "Merges multiple JSON objects or arrays together. Can be used as node module or from command-line.",
  "main": "index.js",
  "bin": {
    "join-json": "bin/join-json.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jamie.peabody@gmail.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/wickedest/join-json.git"
  },
  "keywords": [
    "json",
    "join",
    "merge"
  ],
  "dependencies": {
    "nomnom": "^1.8.1"
  },
  "devDependencies": {
    "chai": "^4.0.2",
    "mocha": "^3.4.2"
  },
  "scripts": {
    "build": "npm run test",
    "test": "NODE_ENV=test mocha ./test --recursive -R spec"
  },
  "license": "MIT"
}
